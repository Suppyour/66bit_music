server {
    listen 80;
    server_name for66bit.ru www.for66bit.ru;

    # Основной прокси на Docker контейнер Бэкенда
    location / {
        proxy_pass http://localhost:8080;
        
        # МАГИЯ SIGNALR (Очень важно для WebSockets!)
        # Без этих настроек телефоны не будут получать сигналы ведущего
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Прокидываем информацию об оригинальном IP клиента
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
