server {
    listen 80;
    server_name for66bit.ru www.for66bit.ru;

    # 1. Запросы к API (Бэкенд)
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 2. Сокеты (SignalR - критичный конфиг)
    location /gameHub {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 3. Фронтенд (Все остальные страницы сайта)
    location / {
        proxy_pass http://localhost:8081; # Обратите внимание, тут порт Фронтенда
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
